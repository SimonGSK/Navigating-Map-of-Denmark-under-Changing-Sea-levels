package org.example;

import java.util.List;

public class TreeNode {
    List<TreeNode> tree
    public List<Tree> find(BoundingBox searchArea) {
        for ()
    }

/*        public List<org.example.TreeNode> find(BoundingBox searchArea) {
            for (org.example.TreeNode n : nodes) {
                BoundingBox nBox = n.element.mbr.box();
                if (nBox.isOverlappingOther(searchArea)) {

                }
            }
        }*/

}
